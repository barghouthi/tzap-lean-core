# Repository conventions

## Naming

- Spell the project, package, crate, and CLI name as lowercase `tzap`.
- For class, type, namespace, and module names that require capitalization, use
  `Tzap`, never `TZap`.
